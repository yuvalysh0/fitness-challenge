# Supabase (for seed script)
# Dashboard → Settings → API → use the SERVICE_ROLE key (secret), NOT the anon key
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_secret_here

# Optional: mock user created by scripts/seed-mock-user.mjs
# MOCK_USER_EMAIL=mock@75hard.local
# MOCK_USER_PASSWORD=MockPass123!
